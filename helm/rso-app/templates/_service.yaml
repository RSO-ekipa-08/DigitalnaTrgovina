{{- define "rso.service" -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}
  namespace: {{ .Values.global.namespace }}
spec:
  selector:
    app: {{ .Values.name }}
  ports:
  - port: {{ .Values.port }}
    targetPort: {{ .Values.port }}
  type: LoadBalancer
{{- end -}} 